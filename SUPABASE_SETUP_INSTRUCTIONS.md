# تعليمات إعداد Supabase لتطبيق Novii

## الخطوة 1: إنشاء الجداول والسياسات

قم بتشغيل الملفات التالية بالترتيب في Supabase SQL Editor:

### 1. تشغيل Schema الرئيسي
افتح ملف `supabase_schema.sql` وانسخ المحتوى بالكامل
- اذهب إلى Supabase Dashboard → SQL Editor
- الصق المحتوى واضغط "Run"

هذا سينشئ:
- ✅ 10 جداول رئيسية (profiles, posts, comments, likes, follows, stories, messages, notifications, saved_posts, story_views)
- ✅ Row Level Security (RLS) policies
- ✅ Indexes للأداء
- ✅ Foreign keys والعلاقات

### 2. تشغيل الدوال المساعدة
افتح ملف `supabase_functions.sql` وانسخ المحتوى
- اذهب إلى Supabase Dashboard → SQL Editor
- الصق المحتوى واضغط "Run"

هذا سينشئ دوال لـ:
- ✅ زيادة/تقليل عدادات الإعجابات
- ✅ زيادة/تقليل عدادات التعليقات
- ✅ زيادة/تقليل عدادات المتابعين
- ✅ زيادة/تقليل عدادات المنشورات

## الخطوة 2: إنشاء أول ملف شخصي

بعد تسجيل الدخول لأول مرة، ستحتاج لإنشاء ملف شخصي:

```sql
-- استبدل USER_ID باستخدام ID المستخدم من auth.users
INSERT INTO profiles (id, username, full_name)
VALUES (
  'YOUR_USER_ID_FROM_AUTH',  -- استبدل هذا
  'username',                 -- اسم المستخدم المطلوب
  'Your Full Name'           -- الاسم الكامل
);
```

أو سيتم إنشاء الملف الشخصي تلقائياً عند أول تسجيل دخول.

## الخطوة 3: إضافة بيانات تجريبية (اختياري)

لاختبار التطبيق، يمكنك إضافة بعض المنشورات:

```sql
-- إنشاء منشور تجريبي
INSERT INTO posts (user_id, caption, image_url, location)
VALUES (
  'YOUR_USER_ID',
  'منشوري الأول على Novii! 🎉',
  'https://picsum.photos/600/800',
  'الرياض، السعودية'
);
```

## التحقق من النجاح

بعد إعداد قاعدة البيانات، يجب أن تكون قادراً على:

- ✅ تسجيل الدخول والخروج
- ✅ رؤية ملفك الشخصي
- ✅ إنشاء منشورات جديدة
- ✅ الإعجاب والتعليق على المنشورات
- ✅ متابعة مستخدمين آخرين
- ✅ إرسال رسائل
- ✅ رؤية الإشعارات

## المتغيرات البيئية المطلوبة

تأكد من إضافة المتغيرات التالية في Replit Secrets:

- `VITE_SUPABASE_URL` - رابط مشروع Supabase
- `VITE_SUPABASE_ANON_KEY` - المفتاح العام لـ Supabase

## استكشاف الأخطاء

### المشكلة: "لا توجد منشورات"
- تأكد من إنشاء منشور واحد على الأقل
- تحقق من RLS policies في Supabase

### المشكلة: "خطأ في الاتصال"
- تأكد من صحة VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY
- تحقق من تشغيل SQL scripts بنجاح

### المشكلة: "خطأ في إنشاء الملف الشخصي"
- تأكد من تشغيل supabase_schema.sql كاملاً
- تحقق من جدول profiles في Supabase

## ملاحظات مهمة

- جميع الجداول محمية بـ Row Level Security
- المستخدمون يمكنهم فقط تعديل بياناتهم الخاصة
- القصص تنتهي تلقائياً بعد 24 ساعة
- التطبيق يستخدم Supabase Auth للمصادقة

---

للمزيد من المساعدة، راجع:
- [Supabase Documentation](https://supabase.com/docs)
- [ملف DATABASE_SETUP.md](./DATABASE_SETUP.md)
